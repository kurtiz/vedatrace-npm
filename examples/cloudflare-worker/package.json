{
	"name": "vedatrace-cloudflare-example",
	"version": "0.0.1",
	"private": true,
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"preview": "wrangler deploy --dry-run"
	},
	"dependencies": {
		"vedatrace": "^0.1.9"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20241127.0",
		"wrangler": "^4.0.0",
		"typescript": "^5.5.0"
	}
}
